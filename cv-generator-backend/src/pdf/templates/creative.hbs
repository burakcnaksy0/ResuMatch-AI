<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>{{fullName}} - CV</title>
    <style>
        @page {
            margin: 0;
            size: A4;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            background: #f3f4f6;
            /* Light gray bg */
            color: #1f2937;
            -webkit-print-color-adjust: exact;
        }

        .container {
            width: 210mm;
            min-height: 297mm;
            margin: 0 auto;
            position: relative;
            background: white;
            display: grid;
            grid-template-columns: 280px 1fr;
            /* Left Sidebar fixed width */
            grid-template-rows: auto 1fr;
        }

        /* HEADER (Spans 2 columns? No, let's keep sidebar distinct) */
        .sidebar {
            grid-row: 1 / -1;
            background: linear-gradient(135deg, #6366f1 0%, #a855f7 100%);
            /* Indigo to Purple */
            color: white;
            padding: 40px 30px;
            display: flex;
            flex-direction: column;
            gap: 30px;
        }

        .profile-pic {
            width: 150px;
            height: 150px;
            border-radius: 20px;
            /* Rounded rect */
            object-fit: cover;
            border: 4px solid rgba(255, 255, 255, 0.3);
            margin: 0 auto;
            display: block;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        .contact-info {
            font-size: 13px;
            display: flex;
            flex-direction: column;
            gap: 12px;
            opacity: 0.9;
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .skill-section h3 {
            font-size: 16px;
            text-transform: uppercase;
            border-bottom: 1px solid rgba(255, 255, 255, 0.3);
            padding-bottom: 10px;
            margin-bottom: 15px;
            letter-spacing: 1.5px;
        }

        .skill-tag {
            background: rgba(255, 255, 255, 0.2);
            padding: 6px 12px;
            border-radius: 30px;
            font-size: 12px;
            display: inline-block;
            margin: 3px;
            font-weight: 500;
        }

        /* MAIN */
        .main {
            padding: 50px;
        }

        .name-header {
            margin-bottom: 40px;
        }

        .name-header h1 {
            font-size: 48px;
            font-weight: 900;
            color: #111827;
            margin: 0;
            line-height: 1;
            letter-spacing: -1px;
            text-transform: uppercase;
        }

        .name-header h2 {
            font-size: 20px;
            font-weight: 300;
            color: #6366f1;
            /* Indigo */
            margin: 10px 0 0 0;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .section {
            margin-bottom: 35px;
        }

        .section-title {
            font-size: 24px;
            font-weight: 800;
            color: #111827;
            position: relative;
            margin-bottom: 25px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .section-title::after {
            content: '';
            height: 4px;
            flex-grow: 1;
            background: linear-gradient(90deg, #6366f1, transparent);
            border-radius: 2px;
        }

        .timeline-item {
            position: relative;
            padding-left: 25px;
            border-left: 3px solid #e5e7eb;
            margin-bottom: 25px;
        }

        .timeline-item::before {
            content: '';
            position: absolute;
            left: -9px;
            top: 0;
            width: 15px;
            height: 15px;
            border-radius: 50%;
            background: #6366f1;
            border: 3px solid white;
            box-shadow: 0 0 0 1px #e5e7eb;
        }

        .job-title {
            font-weight: 700;
            font-size: 16px;
            color: #1f2937;
        }

        .job-meta {
            font-size: 13px;
            color: #6b7280;
            margin-bottom: 8px;
            font-weight: 500;
        }

        .job-desc {
            font-size: 14px;
            color: #4b5563;
            line-height: 1.6;
        }
    </style>
</head>

<body>
    <div class="container">
        <!-- Sidebar -->
        <div class="sidebar">
            {{#if profilePictureUrl}}
            <img src="{{profilePictureUrl}}" class="profile-pic">
            {{/if}}

            <div class="contact-info">
                <div class="contact-item">📧 {{email}}</div>
                {{#if phone}}<div class="contact-item">📱 {{phone}}</div>{{/if}}
                {{#if location}}<div class="contact-item">📍 {{location}}</div>{{/if}}
                {{#if linkedin}}<div class="contact-item">🔗 {{linkedin}}</div>{{/if}}
            </div>

            <div class="skill-section">
                <h3>Skills</h3>
                {{#if skillGroups}}
                {{#each skillGroups}}
                <div style="margin-bottom: 15px;">
                    <div style="font-size: 11px; opacity: 0.8; margin-bottom: 5px; text-transform: uppercase;">
                        {{category}}</div>
                    {{#each skills}}
                    <span class="skill-tag">{{name}}</span>
                    {{/each}}
                </div>
                {{/each}}
                {{else}}
                {{#each skills}}
                <span class="skill-tag">{{name}}</span>
                {{/each}}
                {{/if}}
            </div>

            <div class="skill-section">
                <h3>Education</h3>
                {{#each education}}
                <div style="margin-bottom: 15px; font-size: 13px;">
                    <div style="font-weight: 700;">{{degree}}</div>
                    <div style="opacity: 0.9;">{{institution}}</div>
                    <div style="opacity: 0.7; font-size: 11px;">{{startDate}} - {{#if
                        endDate}}{{endDate}}{{else}}Present{{/if}}</div>
                </div>
                {{/each}}
            </div>
        </div>

        <!-- Main Content -->
        <div class="main">
            <div class="name-header">
                <h1>{{fullName}}</h1>
                <h2>{{jobTitle}}</h2>
            </div>

            {{#if professionalSummary}}
            <div class="section">
                <p style="font-size: 16px; max-width: 90%; color: #374151; font-weight: 400; line-height: 1.8;">
                    {{professionalSummary}}
                </p>
            </div>
            {{/if}}

            {{#if workExperience}}
            <div class="section">
                <div class="section-title">Experience</div>
                {{#each workExperience}}
                <div class="timeline-item">
                    <div class="job-title">{{position}}</div>
                    <div class="job-meta">{{company}} • {{startDate}} - {{#if endDate}}{{endDate}}{{else}}Present{{/if}}
                    </div>
                    <div class="job-desc">{{description}}</div>
                </div>
                {{/each}}
            </div>
            {{/if}}

            {{#if projects}}
            <div class="section">
                <div class="section-title">Projects</div>
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                    {{#each projects}}
                    <div style="background: #f9fafb; padding: 15px; border-radius: 8px; border: 1px solid #e5e7eb;">
                        <div style="font-weight: 700; margin-bottom: 5px; color: #6366f1;">{{name}}</div>
                        <div style="font-size: 12px; color: #4b5563; line-height: 1.5;">{{description}}</div>
                    </div>
                    {{/each}}
                </div>
            </div>
            {{/if}}
        </div>
    </div>
</body>

</html>